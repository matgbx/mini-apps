{
  "name": "challenge_2_csv_maker",
  "version": "1.0.0",
  "description": "takes in JSON data and returns it in CSV format to display for the user",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "jquery": "^3.3.1",
    "require": "^2.4.20"
  }
}
